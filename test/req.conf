

addFile SPEC -path SPEC.docx -tag REQ_[-a-zA-Z0-9_]*
#addFile SPEC -path document.xml -tag REQ_[-a-zA-Z0-9_]*
addFile TEST -path TEST.txt \
    -tag T_[-a-zA-Z0-9_]* \
    -ref "Ref:  *\\\(.*\\\)" \
    -depends-on SPEC SPEC SPEC


