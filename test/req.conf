

addFile SPEC -path SPEC.odt.docx -req REQ_[-a-zA-Z0-9_]* -stop-after Annexe
#addFile SPEC -path SPEC.pdf -req REQ_[-a-zA-Z0-9_]* -stop-after Annexe

#addFile SPEC -path document.xml -req REQ_[-a-zA-Z0-9_]*
addFile TEST -path TEST.txt \
    -req T_[-a-zA-Z0-9_]* \
    -ref "Ref:  *(.*)" \
	-stop-after "Annex" \
	-start-after "Tests cases" 


