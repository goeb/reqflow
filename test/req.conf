
define REQ_PATTERN REQ_[-a-zA-Z0-9_]*

addFile SPEC -path SPEC.docx -req REQ_PATTERN -stop-after Annexe

addFile TEST -path TEST.txt \
    -req T_[-a-zA-Z0-9_]* \
    -ref "Ref:  *(.*)" \
	-stop-after "Annex" \
	-start-after "Tests cases" \
    -nocov


