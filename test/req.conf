
define REQ_PATTERN REQ_[-a-zA-Z0-9_]*
define s SPEC

addFile SPEC -path s.docx -req REQ_PATTERN -stop-after Annexe

#addFile SPEC -path document.xml -req REQ_[-a-zA-Z0-9_]*
addFile TEST -path TEST.txt \
    -req T_[-a-zA-Z0-9_]* \
    -ref "Ref:  *(.*)" \
	-stop-after "Annex" \
	-start-after "Tests cases" \
    -nocov


